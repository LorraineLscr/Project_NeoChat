{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}
{#  <h1>Register</h1> #}
{#  {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }} #}
<div class="container">
    <h2>Veuillez-vous inscrire</h2>
    <br>
    {{form_start(registrationForm)}}
    <div class="form-group">
        {{ form_row(registrationForm.nom, {"label":"Nom : ", "attr":{"placeholder":"", "class":"form-control"}}) }}
    </div>
    <br>
    <div class="form-group">
        {{ form_row(registrationForm.prenom, {"label":"Prénom : ", "attr":{"placeholder":"", "class":"form-control"}})
        }}
    </div>
    <br>
    <div class="form-group">
        {{ form_row(registrationForm.pseudo, {"label":"Pseudo : ", "attr":{"placeholder":"", "class":"form-control"}})
        }}
    </div>
    <br>
    <div class="form-group">
        {{ form_row(registrationForm.email, {"label":"Email : ", "attr":{"placeholder":"", "class":"form-control"}}) }}
    </div>
    <br>
    <div class="form-group">
        {{ form_row(registrationForm.plainPassword, {"label":"Mot de Passe : ", "attr":{"placeholder":"", "class":"form-control"}})
        }}
    </div>
    <br>
    <div class="form-group">
        {{ form_row(registrationForm.confirmPassword, {"label":"Confirmation du Mot de Passe : ", "attr":{"placeholder":"", "class":"form-control"}})
        }}
    </div>
    <br>
    <div class="form-group">
        {{ form_row(registrationForm.avatar, {"label":"Avatar : ", "attr":{"placeholder":"", "class":"form-control"}})
        }}
    </div>
    <br>
    <div class="form-group">
        En cochant cette case j'accepte sans réserve les CGU, ainsi que l'utilisation de mes données personnelles :
        {{ form_row(registrationForm.agreeTerms, {"label":" ", "attr":{ "class":"champ checkbox-inline"}})
        }} <a href="{{path('app_mentions_legales')}}">Voir les CGU</a>
    </div>
    <br>
    <button type="submit" class="btn btn-lg btn-primary">S'inscrire</button>
    {{form_end(registrationForm)}}
    <br>
    <p>Déja inscrit ? Connectez-vous <a href="{{path('app_login')}}">ici</a></p>
</div>
{% endblock %}